const user = {
  name: 'Sean',
  age: 30,
  country: 'Singapore'
};

const { name, age } = user;

console.log(name);
console.log(age);

const colors = ['red', 'blue', 'green'];

const [primary, secondary] = colors;

console.log(primary);
console.log(secondary);
