// 01-foundations-js/11-conditionals/ternary.js

// Syntax: condition ? value_if_true : value_if_false

const age = 20;

const status = 
  age >= 18 
    ? 'Adult'   // Logic executed if true
    : 'Minor';  // Logic executed if false

console.log(status); // Output: 'Adult'
